rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Temporary wide-open rules for testing only.
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
